export interface Transacao {
    valor: number,
    dataHora: string
}

export interface Estatistica {
    count: number,
    sum: number,
    avg: number,
    max: number,
    min: number
}